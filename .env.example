# Base URL of the Quaero backend API
VITE_API_BASE_URL=http://localhost:8080/api

# Set to "true" to use in-memory mocks instead of the real backend.
# Remove or set to "false" once the investigation API is live.
VITE_USE_MOCK_API=true
